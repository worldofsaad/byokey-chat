{
  "name": "@byokey/ai-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest",
    "test:coverage": "vitest --coverage --silent='passed-only'"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "wrangler": "^4.18.0"
  }
}
